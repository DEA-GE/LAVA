name: lava
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - atlite
  - cfgrib
  - gdal
  - geopandas
  - richdem
  - shapely
  - unidecode
  - pyyaml
  - pandas
  - matplotlib
  - numpy
  - pygadm
  - jupyter
  - ipython
  - ipykernel
  - openeo
  - fiona
  - requests
  - openpyxl
  - xdem
  - snakemake
  - leafmap
  - localtileserver
  - pip            # ← ensure pip is installed so we can point at Git if needed
  - pip:
    - OSMPythonTools
    - distancerasters
    - space2stats-client